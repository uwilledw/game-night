

export class Player {
    // name
    // level
    // color

    constructor(name, score, color) {
        this.name == name
        this.score == score
        this.color == color
    }


    check() {
        console.log(`Hi my name is ${this.name}, my score ${this.score} and ${this.color}`)
    }
}